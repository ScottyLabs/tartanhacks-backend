export enum TeamRequestType {
  INVITE = "INVITE",
  JOIN = "JOIN",
}

export enum TeamRequestStatus {
  PENDING = "PENDING",
  ACCEPTED = "ACCEPTED",
  DECLINED = "DECLINED",
  CANCELLED = "CANCELLED",
}
