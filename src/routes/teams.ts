import express, { Router } from "express";
import {} from "../controllers/TeamsController";
import {} from "./middleware";

const router: Router = express.Router();

/**
 * @swagger
 * tags:
 *  name: Teams Module
 *  description: Endpoints to manage team information.
 */

export default router;
